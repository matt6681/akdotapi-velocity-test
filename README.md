# AKDOT API Test Feed

This repository hosts a static test GeoJSON payload for ArcGIS Velocity using GitHub Pages.

Published payload path:

- `docs/gravimon_radar.json`

The GitHub Pages site is configured to publish from the `main` branch `/docs` folder.